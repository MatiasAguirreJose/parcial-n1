#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <limits.h>
#include "funciones.h"

int main(void) {
	setbuf(stdout,NULL);
	int opcion;

	SelecionarMenu();
	do{
	scanf("%d",&opcion);
	MenuPrincipal(opcion);

	}while(opcion != 11);

	return EXIT_SUCCESS;
}
